# 英単語（`courses/英単語/`）

語彙教材の `course.json` をここに置く想定。本文は `text/vocabulary/<教材名>/`、音声は `audio/vocabulary/<教材名>/en` … と `content/README.md` のルールに合わせる。

コア1000 など既存定義は `catalog.json` の `courses` に `core1000` として既に例がある。
